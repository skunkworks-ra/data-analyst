{
  "mcpServers": {
    "ms-inspect": {
      "command": "pixi",
      "args": [
        "run",
        "--manifest-path",
        "${CLAUDE_PLUGIN_ROOT}/pixi.toml",
        "serve"
      ],
      "description": "Radio interferometric MS inspector (stdio transport)"
    }
  }
}
